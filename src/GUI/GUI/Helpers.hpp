#pragma once

namespace GUI::Helpers
{
constexpr float pathTextFieldWidth {400.0f};

const std::string pathTextDefaultLocation {std::filesystem::current_path().string()};

} // namespace GUI::Helpers
